'use client';

// This component is deprecated — chaos controls are now inline in PumpHMI per-pump cards.
// Keeping as a no-op to avoid import errors from any leftover references.

export default function ChaosPanel() {
    return null;
}
